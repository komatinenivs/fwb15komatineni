extends layout

block content
  h1= title
  p Welcome to #{title}. When you #{title} the #{title} the #{title} will ] #{title} you.
  button (type="button" class="btn btn-secondary") Press Me for the contents. 
  
